#version 330 core
// Magnetic sweep trails that drag the cache stack into diagonal current lanes.
in vec2 tc; out vec4 color;
uniform sampler2D samp,samp1,samp2,samp3,samp4,samp5,samp6,samp7,samp8;
uniform sampler1D spectrum0,spectrum1,spectrum2,spectrum3,spectrum4,spectrum5,spectrum6,spectrum7;
uniform float time_f,amp_peak,amp_smooth; uniform vec2 iResolution;
const float TAU=6.28318530718;
vec3 acid(float t){return 0.5+0.5*cos(TAU*(vec3(0.7,1.0,0.8)*t+vec3(0.14,0.36,0.24)));}
vec4 cacheHist(int i, vec2 uv){if(i==0)return texture(samp1,uv);if(i==1)return texture(samp2,uv);if(i==2)return texture(samp3,uv);if(i==3)return texture(samp4,uv);if(i==4)return texture(samp5,uv);if(i==5)return texture(samp6,uv);if(i==6)return texture(samp7,uv);return texture(samp8,uv);} 
float specHist(int i,float f){if(i==0)return texture(spectrum0,f).r;if(i==1)return texture(spectrum1,f).r;if(i==2)return texture(spectrum2,f).r;if(i==3)return texture(spectrum3,f).r;if(i==4)return texture(spectrum4,f).r;if(i==5)return texture(spectrum5,f).r;if(i==6)return texture(spectrum6,f).r;return texture(spectrum7,f).r;}
vec2 shearField(vec2 uv,float bass,float mid,float treble,float air,vec3 oldest,float layer){vec2 dir=vec2(1.0,-0.45); dir+=vec2(sin(uv.y*16.0-time_f*2.4-layer),cos(uv.x*12.0+time_f*1.6+layer))*0.5; dir+=vec2(oldest.g-oldest.r,oldest.b-oldest.g)*0.8; return dir*(0.010+bass*0.026+treble*0.014)+vec2(uv.y,-uv.x)*(0.004+air*0.018);} 
void main(){float aspect=iResolution.x/iResolution.y; vec2 uv=(tc-0.5)*vec2(aspect,1.0); float bass=texture(spectrum0,0.02).r,mid=texture(spectrum0,0.22).r,treble=texture(spectrum0,0.50).r,air=texture(spectrum0,0.80).r; float hist=0.0; for(int i=0;i<8;i++) hist+=specHist(i,0.02); hist/=8.0; vec3 oldest=texture(samp8,tc+vec2(sin(time_f*0.30+uv.y*6.0),cos(time_f*0.18+uv.x*5.0))*(0.012+hist*0.030)).rgb; vec3 live=texture(samp,tc+shearField(uv,bass,mid,treble,air,oldest,0.0)).rgb; live=mix(live,live.gbr,0.20+mid*0.20)*acid(time_f*0.07+uv.x*0.2+bass); vec3 accum=live; float wsum=1.0; for(int i=0;i<8;i++){float layer=float(i+1); float hB=specHist(i,0.02),hM=specHist(i,0.22),hT=specHist(i,0.50),hA=specHist(i,0.80); vec3 cached=cacheHist(i,tc+shearField(uv,hB,hM,hT,hA,oldest,layer)).rgb; float w=pow(0.79,layer)*(1.0+hB*1.3+hM*0.6); accum+=cached*acid(layer*0.12+hB*0.7+oldest.g*0.2)*w; wsum+=w;} accum/=wsum; float streak=smoothstep(0.4,1.0,abs(cos((uv.x+uv.y)*22.0-time_f*2.5+hist*7.0))); accum+=acid(time_f*0.04+uv.y*0.35)*streak*(0.06+amp_smooth*0.20); color=vec4(clamp(mix(accum,vec3(1.0)-accum,smoothstep(0.90,1.0,amp_peak)*0.12),0.0,1.0),1.0);} 